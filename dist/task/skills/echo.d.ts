import type { SkillHandler } from "../types.js";
/**
 * Trivial echo skill. Returns the input verbatim with a single
 * mid-flight progress frame. Useful for protocol smoke tests.
 */
export declare const echoSkill: SkillHandler;
