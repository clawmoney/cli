import type { SkillHandler } from "../../types.js";
/**
 * `x.tweet_retweeters` — backs /TweetRetweeters. Same shape as
 * tweet-likers but a different X GraphQL op underneath.
 */
export declare const xTweetRetweetersSkill: SkillHandler;
