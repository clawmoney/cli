/**
 * Ensures the server is running, installing if needed
 * @returns true if server is running, false on failure
 */
export declare function ensureServerRunning(onInstalling?: () => void, onStarting?: () => void): Promise<boolean>;
/**
 * Checks if the wallet is running
 */
export declare function isWalletRunning(): boolean;
//# sourceMappingURL=serverManager.d.ts.map