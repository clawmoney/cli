/**
 * Address and name validation utilities for EVM and Solana
 */
/**
 * Regex pattern for validating Ethereum addresses (0x followed by 40 hex characters)
 */
export const ETH_ADDRESS_REGEX = /^0x[a-fA-F0-9]{40}$/;
/**
 * Regex pattern for validating Solana addresses (Base58, 32-44 characters).
 * NOTE: Duplicated in apps/payments-mcp chainConfig.ts and packages/mcp-server Operations.ts
 * because those are separate packages that cannot import from this package.
 */
export const SOLANA_ADDRESS_REGEX = /^[1-9A-HJ-NP-Za-km-z]{32,44}$/;
/**
 * Check if a string is a valid Ethereum address
 *
 * @param value - The string to validate
 * @returns true if the value is a valid Ethereum address
 */
export function isEthAddress(value) {
    return ETH_ADDRESS_REGEX.test(value);
}
/**
 * Check if a string is a valid Solana address (Base58 encoded, 32-44 chars)
 *
 * @param value - The string to validate
 * @returns true if the value is a valid Solana address
 */
export function isSolanaAddress(value) {
    return SOLANA_ADDRESS_REGEX.test(value);
}
/**
 * Check if a string looks like an ENS name (contains a dot but isn't a hex address)
 *
 * @param value - The string to validate
 * @returns true if the value looks like an ENS name
 */
export function isEnsName(value) {
    return !isEthAddress(value) && value.includes('.');
}
//# sourceMappingURL=addressValidation.js.map